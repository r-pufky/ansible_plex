---
- name: 'Converge'
  hosts: 'all'
  gather_facts: false
  tasks:
    - name: 'Converge | role | r_pufky.media.plex'
      ansible.builtin.include_role:
        name: 'r_pufky.media.plex'
      vars:
        plex_flg_restore: true
